pipeline {
    agent any

    stages {
        stage("Build"){
            steps {
                script {
                    dockerapp = docker.build("caetanodevops/mentorr-backend-live:${env.BUILD_NUMBER}")
                }
            }
        }
    }
}
