pipeline {
    agent any

    stages {
        stage("Build"){
            steps {
                script {
                    dockerapp = docker.build("caetanodevops/mentorr-backend:${env.BUILD_NUMBER}")
                }
            }
        }
    }
}
