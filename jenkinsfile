pipeline {
    agent any

    stages {
        stage("Build"){
            steps {
                script {
                    docker.build("mentorr-backend:${env.BUILD_NUMBER}")
                }
            }
        }
    }
}
